
    new WOW().init();

    (function () {
  var squareWrapper = document.querySelector('.pip1');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.prod2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.prod2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();


(function () {
  var squareWrapper = document.querySelector('.pip2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.cta');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.second_container');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.info2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.info3');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.deal2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__zoomIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__zoomIn');
        return;
      }

      square.classList.remove('animate__zoomIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap2');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap3');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap4');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap5');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

(function () {
  var squareWrapper = document.querySelector('.wrap6');
  var square = squareWrapper.querySelector('.animate__animated');
  square.classList.remove('animate__fadeIn');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        square.classList.add('animate__fadeIn');
        return;
      }

      square.classList.remove('animate__fadeIn');
    });
  });

  observer.observe(squareWrapper);
})();

